# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Messaging APP' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Messaging APP

  target 'Messaging APPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Messaging APPUITests' do
    inherit! :search_paths
    # Pods for testing
  end
   pod 'Firebase/Core'
   pod 'Firebase/Database'
   pod 'Firebase/Auth'
end
